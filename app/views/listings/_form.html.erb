<%= simple_form_for (listing) do |f| %>
  <%= f.association :book  %>
  <%= f.association :list %>
  <%= f.input :comment %>
  <%= f.submit "Submit to the list", class: "btn btn-primary" %>
<% end %>


<%# this is a great listing for listings/new route %>
