
  <%= simple_form_for [locals[:list], locals[:listing]] do |f| %>
    <%= f.association :book %>
    <%= f.input :comment %>
    <%= f.submit "Submit submit to the list", class: "btn btn-primary" %>
  <% end %>

