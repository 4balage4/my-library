
  <%= simple_form_for [locals[:book], locals[:comment]] do |f| %>
    <%= f.input :comment %>
    <%= f.input :review %>
    <%= f.input :rating %>
    <%= f.submit "Submit comment", class: "btn btn-primary" %>
  <% end %>

<%# [@book, @comment] %>
